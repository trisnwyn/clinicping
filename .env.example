# Copy this file to .env and fill in your real key.
# Never commit .env to git — add it to .gitignore.

GROQ_API_KEY=your_groq_api_key_here

# Optional: change the port the Express server listens on (default 3001)
# PORT=3001
